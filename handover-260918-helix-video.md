# 인계 260918 — 편집기 갤러리 영상 로딩·화질·확대보기

대상: 새 세션의 Claude(채팅·코워크). 클로드 코드용이 아니다 — 필요하면 여기서 클로드 코드용 지시문을 따로 뽑는다.
정본: `editor.html` (원본 편집기 단일 파일, `D:\Claude\대시보드\<날짜>\` 최신 폴더). 아래 행 번호는 260918 마지막 전달본 기준.

## 결정 (근거 포함)

| 결정 | 근거 |
|---|---|
| 나선형 갤러리 2(helixgal) 견본 영상은 Pexels 핫링크를 버리고 **자체 호스팅** | 실서비스에서 방문자 전원이 남의 CDN을 때리는 구조. 주소 바뀌면 전부 깨짐 |
| 저장소 `github.com/creatorlinkapp/editor-assets` `hg/` 폴더, jsDelivr `@main` 으로 서빙 (`BA_ASSET_BASE`, 9467행) | 블럭추가 미리보기(`ba/`)가 이미 같은 방식 |
| 한 영상당 **두 벌**: `hg-NN-sd.mp4`(960×540) 카드용 · `hg-NN-hd.mp4`(1920×1080) 확대보기용, 각 8초 무음 H.264 | 카드는 블럭 폭 35% · 확대 칸은 최대 1400px(레티나 2800). 한 벌로는 둘 다 못 맞춘다 |
| **mp4 단독**, webm 안 씀 | `ba/`에서 webm이 mp4보다 큰 쌍이 다수 · `hgBuild`가 주소 하나만 받음 · iOS Safari WebM은 17.4부터 |
| 영상을 바꿀 때는 **새 이름**으로 올리고 참조만 바꾼다 | jsDelivr `@main` 캐시(최대 7일)가 같은 이름의 옛 파일을 내보냄. 첫 벌 `hg-NN.mp4`(crf 30·900k 상한·화질 부족)는 이 이유로 이름을 바꿔 다시 올리고 옛 12편은 지웠음 |
| 인코딩: sd crf 23 상한 없음 · hd crf 22 상한 14M | 콘서트 영상(조명·군중)은 상한을 걸면 뭉개진다. hd 는 jsDelivr 파일당 20MB 제한 안 |
| 확대보기는 **sd로 즉시 열고 hd가 재생 가능해지면 같은 시점에서 교체** | jsDelivr 는 지역마다 첫 방문자가 파일당 콜드 3초를 겪음. 워밍은 엣지별·기한부라 무의미 → 콜드여도 안 보이게 |
| 확대보기 컨트롤은 **정지/재생 · 진행바(스크럽) · 소리** 셋만. 브라우저 기본 `controls` 안 씀 | 사용자가 소리 있는 긴 영상을 넣으면 소리도 못 켜고 멈추지도 못했음. 기본 controls 는 생김새 제각각 + 볼륨·전체화면·속도·PiP 딸려옴 |
| 컨트롤은 확대보기에만. 카드는 무음 루프 그대로 | 본인 결정 |
| 로딩 스피너·shimmer·포스터 안 넣음 | 예열 + 자체 호스팅으로 로딩 체감이 사라져 필요 없어짐 |
| Pexels API 로 저화질 주소 뽑는 안은 **폐기** | 자체 호스팅과 화질·첫 프레임이 같은 급이면서 루프 트래픽만 크고 외부 의존 남음 |

## editor.html 에 들어간 변경 (260918)

| 무엇 | 어디 | 요약 |
|---|---|---|
| hg 영상 예열 `hgWarm` | `hgPlace` 앞(약 13106~13130) · `hgBuild` 끝 · `hgInit` S 선언 · `hgInit` tick | 정면 앞 5장을 두 편씩 `preload='auto'`+`load()`, `loadeddata`까지 대기, 4초 타임아웃. `S.vis` 검사보다 앞에서 돌아 블럭이 화면에 들어오기 전부터 받음 |
| `HG_VIDS` | 12943~12958 | `BA_ASSET_BASE+'hg/hg-NN-sd.mp4'` 12줄 |
| `hgLbSrc(u)` | 12936 | `BA_ASSET_BASE+'hg/'`로 시작하는 주소만 `-sd.mp4`→`-hd.mp4`. 사용자 주소는 그대로 |
| 라이트박스 컨트롤 + hd 교체 | CSS 1218~1228 · JS 12685~12750(`igLbSnd`·`IG_LB_ICO`·`igLbSync`·`igLbSeek`·`igLbUpgrade`·`igLbMedia`) · `igLbEl` 12770~12783 · `igLbClose` 12825 | `igLbOpen({hd:fn})`을 주면 교체. 영상 위 탭 = 정지/재생, 스페이스 = 정지/재생, 닫기는 X·배경·Esc. 소리 상태는 다음/이전에 이어지고 닫으면 무음으로 |
| hg 확대 호출 | 13161 | `hd:hgLbSrc` |
| `VC_VIDS`(세로 스크롤 갤러리) | 14148~14163 | 같은 12편이라 `hg/` sd 재사용. 순서는 예전 그대로(4번째=`hg-11`, 11번째=`hg-04`) |
| vc 확대 호출 | 14526 | `hd:hgLbSrc` |
| 도크 주석 새어 나옴 5곳 | 5947·6138·6280·6386·6492 | HTML 본문의 `/* 260917 추가 — 클릭 시 확대 */` → `<!-- … -->` (dg·wc·ss·vc·hx) |

## 저장소 상태

- `editor-assets/hg/` 24편 푸시 완료(sd 합계 22MB · hd 합계 78MB · hd 최대 `hg-03` 10.9MB). 옛 `hg-NN.mp4` 삭제됨
- `_make_hg.ps1` 저장소 루트. 원본은 `_src\hg\`(`.gitignore`). 실행: `powershell -ExecutionPolicy Bypass -File .\_make_hg.ps1` (ffmpeg 필요: `winget install Gyan.FFmpeg`)
- hg ↔ pexels id: 01 31540264 · 02 28551470 · 03 31540263 · 04 12719510 · 05 13082773 · 06 31540267 · 07 30805503 · 08 6174198 · 09 26620363 · 10 30815459 · 11 31342504 · 12 6174235

## 검증 상태

- 모든 변경: `node --check` 인라인 4블럭 통과 + Playwright(예열 큐 · 주소 매핑 · 컨트롤 · 스크럽 · hd 교체 · 닫기)
- **본인 실측**: Disable cache 로 카드 로딩 지연 없음 확인 · 확대보기 첫 열기 3초는 jsDelivr 콜드(두 번째부터 빠름)
- **미실측**: 컨트롤 셋과 sd→hd 교체를 실제 jsDelivr 영상으로는 아직 안 봄(작성 환경에서 jsDelivr·pexels 차단) — 다음 세션 첫 확인 항목
- 스크럽은 서버가 Range 요청을 받아야 한다. jsDelivr·GitHub 는 됨. 사용자 서버가 안 받으면 0초로 튐(코드로 못 막음)

## 열린 항목

1. 세로 스크롤 갤러리(vc)에 예열 없음 — `preload="metadata"` + `vnear` 때 `play()`. 카드가 세로로 쌓여 정도는 덜할 것. 보고 판단
2. hg 동시 재생: `hgPlace` `|q|<2.2` 라 정면 5장 동시 재생. iOS 동시 디코딩 제한에 불리. 1~2장으로 좁히는 안 — 눈으로 보고 판단
3. hg 0~2번 카드는 블럭 생성 즉시 `play()` 라 예열 여유 없음. 첫 등장 깜빡임은 남을 수 있음
4. sd 합계 22MB — 정면 6~7편 예열이면 첫 방문 12~15MB. 로딩 체감이 다시 나오면 sd만 crf 25로(20% 감소)
5. `hgLbSrc` 이름이 hg 전용처럼 보이지만 실제로는 `hg/` 접두어 검사만 한다. vc 도 이걸 쓴다. 다른 블럭이 늘면 이름을 공용으로 바꿀 후보

## 경계·작업 규칙

- 원본 `editor.html`·`index.html`은 저장소에 넣지 않고 본인이 날짜 폴더에 직접 넣는다. 정본은 항상 최신 날짜 폴더
- 한국어 · 미니멀 UI(옵션 추가 전에 "꼭 필요할까") · `/* 260918 수정 — 이유 */` 주석 · 정확 일치 문자열 치환 · `node --check` + Playwright · 변경은 표로 보고
- 산출 파일명 영문. 다른 AI 지시문은 결정·근거·경계만, 클로드 코드용/디자인용 명시
- 토큰·키는 대화창에 남기지 않는다
- Claude 데스크탑은 다른 계정 — 대화·메모리가 따라가지 않으니 파일과 지시문은 자족적으로
